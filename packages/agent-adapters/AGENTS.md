# Agent Adapters Scope

This package is parked future infrastructure for transcript/session ingestion.

Use it when paid session ingestion returns to scope.

For v1, local agent target knowledge belongs in `crates/rudel-local` and TypeScript compiler target logic belongs in `packages/skill-compiler`.
