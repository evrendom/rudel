# Agent Adapters Scope

This package is dormant future infrastructure for transcript/session ingestion.

Do not use it in the v1 Skill Blueprint runtime unless explicitly asked.

For v1, local agent target knowledge belongs in `crates/rudel-adapters` and TypeScript compiler target logic belongs in `packages/skill-compiler`.
