<div class="use-case-strip" role="tablist" aria-label="Dashboard use case">
	<button
		type="button"
		role="tab"
		aria-selected="true"
		aria-controls="scene-data"
		id="tab-data"
		data-scene-tab="data"
	>
		Data
	</button>
	<span aria-hidden="true"></span>
	<button
		type="button"
		role="tab"
		aria-selected="false"
		aria-controls="scene-reporting"
		id="tab-reporting"
		tabindex="-1"
		data-scene-tab="reporting"
	>
		Reporting
	</button>
</div>

<style>
	.use-case-strip {
		position: absolute;
		z-index: 8;
		bottom: calc(100% + 14px);
		left: 50%;
		display: flex;
		align-items: center;
		gap: 3px;
		padding: 4px;
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.9);
		box-shadow: 0 1px 2px rgba(17, 18, 20, 0.04),
			0 8px 24px rgba(17, 18, 20, 0.08), 0 0 0 1px rgba(17, 18, 20, 0.06);
		transform: translateX(-50%);
		backdrop-filter: blur(16px);
	}

	button {
		height: 32px;
		padding: 0 11px;
		border: 0;
		border-radius: 999px;
		background: transparent;
		color: var(--color-ink-muted);
		font-size: 12px;
		font-weight: 500;
		line-height: 32px;
		cursor: pointer;
	}

	button[aria-selected="true"] {
		background: #18191b;
		color: #fff;
	}

	span {
		width: 1px;
		height: 16px;
		margin: 0 2px;
		background: var(--color-line);
	}

	@media (max-width: 767px) {
		.use-case-strip {
			display: none;
		}
	}
</style>
